{
  "name": "hello-world-plugin",
  "version": "1.0.0",
  "description": "",
  "main": "./out/extension.js",
  "scripts": {
    "watch": "tsc -watch -p ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/vscode": "^1.62.0",
    "typescript": "^4.5.2"
  },
  "engines": {
    "vscode": "^1.22.0"
  },
  "activationEvents": [
    "onCommand:ferudinato.helloWorld"
  ],
  "contributes": {
    "commands": [
      {
        "command": "ferudinato.helloWorld",
        "title": "Saluda al mundo"
      }
    ]
  }
}
